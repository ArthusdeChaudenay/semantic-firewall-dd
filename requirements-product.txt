# Product surface (product/): FastAPI app + connectors + report generators.
# NOT required to reproduce any paper result — install only to run the API.
-r requirements.txt
fastapi==0.138.0
uvicorn==0.49.0
pydantic==2.13.4
starlette==1.3.1
httpx==0.28.1

"""
Evaluation harness.

Honest evaluation requires external ground truth (D2/D3): see
``xbrl_ground_truth`` (E0). Metrics for the unsupervised error detector
(ROC-AUC, PR-AUC, precision@k, calibration) live in ``metrics`` (E1).
"""
